{
  "name": "punch-arena",
  "private": true,
  "packageManager": "pnpm@10.12.4",
  "scripts": {
    "dev": "pnpm --dir apps/web dev",
    "build": "pnpm --dir apps/web build",
    "lint": "pnpm --dir apps/web lint",
    "typecheck": "pnpm --dir apps/web typecheck",
    "dao:build": "cargo build -p token -p governor -p treasury --release --target wasm32v1-none",
    "dao:build:mercury": "cargo build -p token -p governor -p treasury --release --target wasm32v1-none --features mercury",
    "dao:test:e2e": "cargo test -p dao-e2e"
  },
  "workspaces": [
    "apps/*",
    "packages/*",
    "packages/*/*"
  ]
}
