[package]
name = "dao-governor"
version = "0.0.0"
edition = "2021"
publish = false

[lib]
crate-type = ["lib", "cdylib"]
doctest = false

[dependencies]
soroban-sdk = { workspace = true }
stellar-governance = { workspace = true }
retroshade-sdk = { version = "0.1.0", optional = true }

[dev-dependencies]
soroban-sdk = { workspace = true, features = ["testutils"] }
dao-token = { path = "../token", features = ["testutils"] }
dao-treasury = { path = "../treasury", features = ["testutils"] }

[features]
mercury = ["dep:retroshade-sdk"]
testutils = ["soroban-sdk/testutils"]
